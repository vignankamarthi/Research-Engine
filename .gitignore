# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Personal Claude Code overrides for this project (cluster-command allow-list)
.claude/settings.local.json

# Python / build
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/
artifacts/

# Agentic 3-file + the working plan (never committed, per the agentic file protocol)
CLAUDE.md
MEMORY.md
ANTIPATTERNS.md
PLAN.md

# Internal venue/submission strategy (never committed; the repo is public)
VENUE-TIMELINE.md

# COMMITS.md is intentionally NOT ignored here: it stays untracked, runs the commits, then is deleted.
